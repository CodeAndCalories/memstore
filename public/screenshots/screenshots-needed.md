# Screenshots to capture before listing

Save PNGs into this folder (`public/screenshots/`) and reference them in `README.md` and listing copy.

Recommended shots (1280×800 or larger):

1. **landing-hero.png** — homepage (`https://memstore.dev/`)
2. **quickstart-docs.png** — `/quickstart.html` docs page
3. **api-reference.png** — `/guide.html` or one of the docs sub-pages
4. **terminal-curl.png** — terminal showing successful `curl POST /v1/memory/remember` + `GET /v1/memory/recall` round-trip
5. **python-sdk.png** — code editor with the Python SDK 3-line example (`import → remember → recall`)
6. **mcp-claude-desktop.png** — Claude Desktop showing the Memstore MCP tools listed (`remember`, `recall`, `forget`)
7. **blog-list.png** — the blog index showing all 10 SEO posts
8. **admin-dashboard.png** — `/admin.html` with sample agents/usage

Tips:
- For terminal shots, use a clean iTerm + truecolor theme; redact real API keys
- For the MCP shot, show the `mcpServers` config block side-by-side with the available tools
- Compress with `tinypng.com` before commit
